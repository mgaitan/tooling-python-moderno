# Tooling moderno en Python

Slides sobre DevEx y tooling moderno en Python, con foco en `uv`, `uvx` y `ruff`. 

- Slides online: https://mgaitan.github.io/tooling-python-moderno/

## Uso local

```bash
make build
make open
make deploy
```

El build usa Sphinx + MyST + RevealJS via `uvx`.
